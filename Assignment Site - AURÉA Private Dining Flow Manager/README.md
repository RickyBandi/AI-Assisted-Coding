# LinkedScape (React)

Apple‑style linked list visualizer rebuilt with React + motion.

## Run locally

```bash
npm install
npm run dev
```

Then open the URL shown in the terminal.

## Notes

- The old static files (`style.css`, `script.js`) are still in the folder, but the app now runs from `src/`.

